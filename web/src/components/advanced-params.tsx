"use client";
import type { JobOptions } from "@/lib/jobs";

export function AdvancedParams({
  options,
  onChange,
}: {
  options: JobOptions;
  onChange: (o: JobOptions) => void;
}) {
  void options;
  void onChange;
  return null;
}
